name: Test
on:
  pull_request:
  push:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Echo
      run: |
        echo ${{ env.branch_name }}
        echo ${{ env.ref_name }}
