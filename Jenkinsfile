pipeline {
  agent any
  stages {
    stage('Build') {
      stage('Build') {
        steps {
          echo 'Build step running'
        }
      }
    }
    stage('Test') {
      steps {
        echo 'Test step running'
      }
    }
  }
}

