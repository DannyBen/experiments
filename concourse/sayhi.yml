---
platform: linux

image_resource:
  type: docker-image
  source: {repository: alpine}

inputs:
- name: experiments-repo

run:
  path: ls experiments-repo/concourse # experiments-repo/concourse/so_something.sh
